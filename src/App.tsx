import { DotGridBackground } from "@HOC/dotGridBackground";
import { Workflow } from "@pages/workflow";

const App = () => {
	return (
		<DotGridBackground>
			<Workflow />
		</DotGridBackground>
	);
};

export default App;
